#include <stdlib.h>
/* more headers goes there */
/* main - print the whole alphabet
 * *Return: zero
 * */
int main(void)
{
for (ch= 'a'; ch<= 'z'; ch++)
putchar (ch);
putchar ('\n') ;
getch();
return (0);
}
