#include <stdlib.h>
/* more headers goes there */
/* main - print the whole alphabet in upper and lowercase
*Return: zero
*/
int main(void)
{
for (ch = 'a'; ch <= 'z'; ch++)
{
putchar (ch'\n');
getch();
return (0);
}
for (CH = 'A'; CH <= 'Z'; ch++)
{
putchar(CH'\n');
getCH();
}
}
