#include "main.h"
#include <ctype.h>
/**
 *_isupper - check for upper case
 *@c : int
 *Return: 1 if upper case, 0 if else
 */
int _isupper(int c)
{
if (isupper(c))
{
return (1);
}
else
{
return (0);
}
}
